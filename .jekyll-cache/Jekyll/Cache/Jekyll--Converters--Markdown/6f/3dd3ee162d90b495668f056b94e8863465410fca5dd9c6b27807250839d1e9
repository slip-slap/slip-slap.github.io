I"Í
<h4 id="tqdm">tqdm</h4>
<ul>
  <li>tqdm: means progress in Arabic</li>
  <li>function: instantly make your loops show a progress meter -  just wrap any iterable with tqdm <strong>tqdm(iterable)</strong>, and you are done</li>
</ul>

<h4 id="useage">useage</h4>

<ol>
  <li>command line mode</li>
</ol>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">seq </span>100000000 | tqdm <span class="nt">--total</span> 100000000 | <span class="nb">wc</span> <span class="nt">-l</span>
</code></pre></div></div>

<ol>
  <li>python code mode</li>
</ol>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="nn">tqdm</span> <span class="kn">import</span> <span class="n">tqdm</span>

<span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="n">tqdm</span><span class="p">(</span><span class="nb">range</span><span class="p">(</span><span class="mi">1000000</span><span class="p">)):</span>
    <span class="k">pass</span>
</code></pre></div></div>

<h4 id="reference">reference</h4>
<ol>
  <li><a href="https://tqdm.github.io/">tqdm</a></li>
</ol>

:ET