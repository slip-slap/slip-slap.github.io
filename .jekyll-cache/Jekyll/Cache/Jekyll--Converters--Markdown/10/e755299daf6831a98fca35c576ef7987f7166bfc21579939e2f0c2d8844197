I"
<h4 id="example">Example</h4>

<div class="language-haskell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kr">data</span> <span class="kt">List</span> <span class="n">a</span> <span class="o">=</span> <span class="kt">Nil</span> <span class="o">|</span> <span class="kt">Cons</span> <span class="n">a</span> <span class="p">(</span><span class="kt">List</span> <span class="n">a</span><span class="p">)</span>
</code></pre></div></div>

<ol>
  <li>Replace $\textbf{List a}$  with $\textbf{x}$, we get the equation
    <ul>
      <li>$x = 1 + a \times x$</li>
    </ul>
  </li>
  <li>We can’t solve it using traditional algebraic methods because we can’t
substract or divide types. But we can try a series of substitutions, where we
keep tracking x on the right hand with $1 + a \times x$, and use the
distributive property</li>
</ol>

<p>$
\begin{array}{l}x=1+a * x \ x=1+a *(1+a * x)=1+a+a * a * x \ x=1+a+a * a *(1+a</p>
<ul>
  <li>x)=1+a+a * a+a * a * a * x\end{array}
$</li>
</ul>

:ET