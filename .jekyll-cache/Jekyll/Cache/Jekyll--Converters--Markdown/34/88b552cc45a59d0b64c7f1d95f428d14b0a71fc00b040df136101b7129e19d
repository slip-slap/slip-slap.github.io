I"¦<h4 id="conditional-blocks">Conditional Blocks</h4>

<div class="language-cmake highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">if</span><span class="p">()</span>/<span class="nb">elseif</span><span class="p">()</span>/<span class="nb">else</span><span class="p">()</span>/<span class="nb">endif</span><span class="p">()</span>
</code></pre></div></div>

<h4 id="loops">Loops</h4>

<div class="language-cmake highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">foreach</span><span class="p">()</span>/<span class="nb">endforeach</span><span class="p">()</span> 
<span class="nb">while</span><span class="p">()</span>/<span class="nb">endwhile</span><span class="p">()</span>
</code></pre></div></div>

<h4 id="command-definitions">Command Definitions</h4>

<div class="language-cmake highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">macro</span><span class="p">()</span>/<span class="nb">endmacro</span><span class="p">()</span>
<span class="nb">function</span><span class="p">()</span>/<span class="nb">endfunction</span><span class="p">()</span>
</code></pre></div></div>

:ET