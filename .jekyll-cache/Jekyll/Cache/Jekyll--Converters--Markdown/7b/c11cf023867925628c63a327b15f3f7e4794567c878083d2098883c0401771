I"�<h4 id="list">List</h4>
<p>The lisb subcommands <strong>APPEND INSERT FILTER REMOVE_ITEM REVERSE SORT</strong> may create new values for the
list within the current CMake variable scope.</p>
<ul>
  <li>A list in cmake is a <strong>;</strong> separated group of strings. For example, <strong>set(var a b c d e)</strong> creates
a list with <strong>a;b;c;d;e;</strong>, and set <strong>set(var “a b c d e”)</strong> creates a string or a list with one
item in it.</li>
</ul>

:ET