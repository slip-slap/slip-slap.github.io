I"�
<h4 id="background">Background</h4>
<ol>
  <li>f(x,y) = (u,v) where u = u(x,y) and v = v(x,y)</li>
  <li>f(z) = w where z and w are complex numbers
What’s the difference between the equations above ? No difference at all.
We can do conformal mapping with complex number ?
    <ul>
      <li>The reason we need this because comlex numbers give us some advantage when we
do comformal mapping.</li>
      <li>Conformal mappling doesn’t belong to complex number, just based on the
properties of complex number, we can do conformal mapping better.</li>
    </ul>
  </li>
</ol>

<h4 id="note">Note</h4>
<ol>
  <li>conformal: leaving the size of the angle between corresponding curves
unchanged &lt;conformal transformation&gt;</li>
</ol>

:ET